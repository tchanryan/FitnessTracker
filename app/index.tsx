import { StyleSheet, View, Text } from 'react-native';
import Globalstyle from './constants/styles';

const Home = () => {
    return (
        <View>
            <Text style={Globalstyle.heading}>Hello, World!</Text>
        </View>
    );
};

export default Home;
